<% include('./layout'); %>

<h2>게시글 목록</h2>
<ul>
  <% posts.forEach(post => { %>
  <li>
    <a href="/posts/<%= post.id %>"><%= post.title %></a>
    <small
      >by <%=post.author %> on <%= new Date(post.createdAt).toLocaleString()
      %></small
    >
  </li>
  <% }) %>
</ul>
